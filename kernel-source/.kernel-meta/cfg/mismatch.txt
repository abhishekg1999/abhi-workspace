    [NOTE]: 'CONFIG_VIRTIO' last val (m) and .config val (y) do not match
    [INFO]: CONFIG_VIRTIO : y ## .config: 4675 :configs//cfg/openamp.cfg (m) 
    [INFO]: raw config text:

        config VIRTIO
        	tristate
        	select VIRTIO_ANCHOR
        	help
        	  This option is selected by any driver which implements the virtio
        	  bus, such as CONFIG_VIRTIO_PCI, CONFIG_VIRTIO_MMIO, CONFIG_RPMSG
        	  or CONFIG_S390_GUEST.

    [INFO]: config 'CONFIG_VIRTIO' was set, but it wasn't assignable, check (parent) dependencies

    [INFO]: selection details for 'CONFIG_VIRTIO':
        Symbols currently y-selecting this symbol:
          - REMOTEPROC

        Symbols currently m-selecting this symbol:
          - RPMSG_VIRTIO

        Symbols currently n-selecting this symbol (no effect):
          - CAIF_VIRTIO
          - VIRTIO_CONSOLE
          - I2C_VIRTIO
          - DRM_VIRTIO_GPU
          - VIRTIO_PCI
          - VIRTIO_VDPA
          - VIRTIO_MMIO
          - VIRTIO_FS


